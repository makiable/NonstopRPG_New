using UnityEngine;
using System.Collections;
using System.Xml;


public class xmltest02 : MonoBehaviour {

	//새로운 xml 문서를 만듭니다.
	//System.Xml.XmlDocument Document = new System.Xml.XmlDocument;

	//새로운 요소를 만듭니다. Element는 요소, 성분이라는 뜻입니다.
	//XmlElement myElement = Document.createElement("item");

	//요소에 속성을 지정합니다. 속성의 이름과 값이 들어갑니다.
	//myElement.setAttribute("name", "아이템");ㅣ

	//요소로 부터 값을 가져옵니다. GetAttribute("name"); 반환된 값은 문자 열입니다.
	//itemElement.GetAttribute("Name");

	//문서에 첨부 합니다. append는 첩부라는 뜻입니다.
	//Document.AppendChild(itemListElement);

	// 문서를 저장합니다.
	//XmlDocument.Save(filePath);




	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
